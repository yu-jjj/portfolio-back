# node_modules 폴더는 용량이 크고, 각자 설치하도록 해야 하므로 무조건 제외
node_modules/

# 환경변수 파일(민감 정보)
.env

# 
uploads

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제별 불필요 파일
.DS_Store
Thumbs.db

# IDE/에디터 설정 파일
.idea/
.vscode/

# 빌드 결과물(필요시)
dist/
build/

.history/

# 패키지 매니저 lock 파일 (yarn.lock은 보통 포함, package-lock.json은 yarn 사용시 불필요)
package-lock.json
package-lock-copy.json

# 기타 불필요한 파일
*.local

# lock files
yarn.lock